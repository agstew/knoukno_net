extends layout

block content
  h2 Question Average
  if avgRating
    p Average rating: #{avgRating.toFixed(2)}
  else
    p No ratings yet.
  p Total ratings: #{count}
  a(href='/admin/questions') Back
